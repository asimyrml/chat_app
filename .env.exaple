###############################################################################
#               🚧  Chat App – Ortam Değişkenleri Örnek Dosyası              #
#  Bu dosyayı `.env` ismiyle kopyalayın ve **gerçek** değerlerle doldurun.    #
#  (Versiyon kontrolüne **eklemeyin** – .gitignore’da zaten filtrelenmiş.)    #
###############################################################################

########################
# 🔑  Django Ayarları  #
########################
# 50+ karakterlik rastgele dize üretin (`python -c "import secrets, string, random; print(''.join(random.choice(string.ascii_letters+string.digits) for _ in range(50)))"`)
SECRET_KEY=replace-me

# Production’da false olmalı
DEBUG=true
# Domain(ler) virgülle ayırın, local’de * kullanabilirsiniz
ALLOWED_HOSTS=localhost,127.0.0.1

###############################
# 🗄️  Veritabanı (PostgreSQL) #
###############################
# Format → postgres://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgres://chatuser:chatpass@db:5432/chatdb

####################
# ⚡  Redis (Cache) #
####################
REDIS_URL=redis://redis:6379/0

##############################
# ✉️  E-posta Gönderimi (SMTP)
##############################
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=true
EMAIL_HOST_USER=donotreply@example.com
EMAIL_HOST_PASSWORD=replace-me
DEFAULT_FROM_EMAIL=Chat App <donotreply@example.com>

###################################
# 🌐  Frontend – API Base URL      #
###################################
# React tarafında Vite ‘import.meta.env’ ile kullanılır
VITE_API_URL=http://localhost:8000/api

############################
# 📝  Diğer Opsiyoneller    #
############################
# LOG_LEVEL=INFO
# SENTRY_DSN=https://xxxxxxxx@o0.ingest.sentry.io/0
